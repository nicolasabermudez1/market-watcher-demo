# Template for Streamlit Cloud secrets — DO NOT COMMIT a real secrets.toml.
# On Streamlit Cloud: paste the contents below into App Settings → Secrets.
# Locally: copy this to .streamlit/secrets.toml OR (preferred) use the root .env file.

GEMINI_API_KEY = "AIzaSy..."

# Optional overrides:
# GEMINI_MODEL_PRIMARY = "gemini-2.5-flash"
# GEMINI_MODEL_FAST = "gemini-2.5-flash-lite"
