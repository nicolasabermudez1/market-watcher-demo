[models]
primary = "gemini-2.5-flash"
fast = "gemini-2.5-flash-lite"
embedding = "text-embedding-004"

[tools]
use_web_search = false
use_chroma = false
use_brave_fallback = false

[demo]
category = "IT Software"
category_subdir = "it_software"
suppliers = ["microsoft", "sap", "oracle", "salesforce", "servicenow", "workday", "atlassian", "pega", "snowflake"]
spend_threshold_gbp = 50000

[paths]
fixtures = "data/fixtures"
runs = "data/runs"
outputs = "outputs"
state_db = "data/state.sqlite"
